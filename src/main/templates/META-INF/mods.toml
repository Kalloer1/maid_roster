modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="女仆点名册"
logoFile="maid_roster.png"
authors="${mod_authors}"
description='''《女仆点名册》——车万女仆附属。可绑定任意多名女仆，一键把她们全部集合到身边（支持跨维度与未加载区块），并通过统一管理界面批量设置工作模式与作息时间。'''

[[dependencies.${mod_id}]]
modId="forge"
mandatory=true
versionRange="${forge_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
mandatory=true
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="touhou_little_maid"
mandatory=true
versionRange="${maid_version_range}"
ordering="AFTER"
side="BOTH"
